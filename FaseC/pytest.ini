[pytest]
# rootdir = FaseC/. `pythonpath = .` rende `phase_c` importabile da qualunque cwd:
# e' esattamente cio' che permette alle due facciate (script e notebook) di eseguire
# LO STESSO codice, che e' il presupposto del cancello di parita'.
testpaths = tests
pythonpath = .
addopts = -q
