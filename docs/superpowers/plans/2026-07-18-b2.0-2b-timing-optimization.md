# Fase B2.0-2b — Ottimizzazione timing di `Donatello_ACC_IIDM_M` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Alzare l'`Fmax` OOC del controllore `Donatello_ACC_IIDM_M` oltre i 9,30 MHz attuali mantenendo `dmax = 0` (bit-identità col blocco), decidendo il target sui dati di un probe make-or-break.

**Architecture:** Il collo è il `tanh` nativo dello stadio `phase==6` della chart-FSM (207 livelli). Non è spezzabile a mano nel sorgente (operatore atomico), quindi l'unica leva bit-exact è il pipelining **automatico** di HDL Coder *dentro* quel datapath. Un probe OOC (VHDL, `synth_acc_iidm.tcl`) misura se il tool ci riesce; se sì, la config vincente si applica al blocco committato e la non-regressione la provano gli harness B2.0 esistenti (il testbench assorbe la latenza < `HOLD=500`).

**Tech Stack:** MATLAB/Simulink + HDL Coder R2026a (`makehdl`, `hdlset_param`), Vivado 2026.1 OOC (`scripts/synth_acc_iidm.tcl`, xc7z020), xsim (bit-exact), harness MATLAB esistenti (`run_rtl_validate_b`, `run_rtl_validate`, `run_plant_par`, `run_closed_loop`, `run_b2_parity_dataset`).

**Spec:** `docs/superpowers/specs/2026-07-18-b2.0-2b-timing-design.md`

> **⚠️ ESITO F1 (2026-07-18): FAIL — probe eseguito, verdetto misurato.** Il `tanh` nativo fixed NON è
> pipelinabile bit-exact da alcuno strumento automatico: HDL Coder `DistributedPipelining`/`ClockRatePipelining`
> mettono i registri all'uscita (barriera stateful-chart) → **0%**; il retiming di Vivado (op4/80ns e op8/40ns,
> identici) → **+2,4% (9,52 MHz), tetto**. Tabella e perché in `document/SP4_ACC_IIDM_FAST.md §Studio 2b`.
> **→ Le Fasi 2 / 2-bis / 3 di questo piano NON si eseguono così come sono.** L'utente ha deciso 2 esperimenti
> **manuali** (reimplementare il `tanh` · inserire registri a mano nel netlist generato definitivo, bit-exact) →
> **nuovo brainstorming/spec il 2026-07-19**. Questo piano resta il record fedele della via automatica (probe-first).

**Note operative trasversali (valgono per ogni task):**
- Working dir: `D:\Project_MBSE\1.Reti Neurali\Rete_SNN_Test\CF_FSNN\.worktrees\Simulink_Importer` (branch `Simulink_Importer`).
- MATLAB si lancia dalla cartella `matlab/`. Forma batch: `matlab -batch "cd('matlab'); <chiamata>"`.
- Vivado: `& "C:\AMDDesignTools\2026.1\Vivado\bin\vivado.bat" -mode batch -source scripts\synth_acc_iidm.tcl -tclargs <srcdir> <outdir> <label>` (o `vivado` se `settings64` è già nel PATH). Clock fisso 125 ns = 8 MHz; il top sintetizzato è `DUT`.
- **Core SNN**: NON si tocca la matematica di alcun sorgente. F1 lavora solo su attributi HDL del blocco M (fuori dal core). F2 (SNN→decode) è nel core ed è **conditional**, gated su F1 + `run_b2_parity_dataset` 0/240000.
- Commit senza `Co-Authored-By` (convenzione repo).

---

## Fase 0 — Fidarsi del flusso: baseline OOC attraverso il probe

Obiettivo: dimostrare che il flusso "scratch model → VHDL → `synth_acc_iidm.tcl`" riproduce il numero noto del blocco M (~8614 LUT, ~9,30 MHz, critpath sul `tanh`). Se il baseline non torna, ogni misura successiva è inaffidabile.

### Task 0.1: Script del probe di pipeline (solo generazione VHDL)

**Files:**
- Create: `matlab/probe_pipe_tanh.m`

- [ ] **Step 1: Scrivere `matlab/probe_pipe_tanh.m`**

```matlab
function probe_pipe_tanh(cfgs)
%PROBE_PIPE_TANH  [B2.0-2b F1, make-or-break] HDL Coder riesce a PIPELINARE bit-exact il tanh nativo
%  (stadio phase==6 di Donatello_ACC_IIDM_M, 207 livelli) coi suoi attributi automatici? Genera il VHDL
%  del blocco M in piu' config di pipeline, in cartelle separate (matlab/hdl_pipe/<tag>), per la sintesi
%  OOC (scripts/synth_acc_iidm.tcl -> RESULT/CRITPATH = il verdetto). NON tocca la libreria committata:
%  lavora su un modello scratch m_pipe_<tag> con una COPIA del blocco (LinkStatus=none), come
%  probe_acciidm_sharing.
%
%  Leve (attributi HDL Coder, NON sorgente: il tanh e' un operatore nativo atomico, non spezzabile a mano):
%   - OutputPipeline (subsystem): N registri sull'uscita del DUT.
%   - DistributedPipelining (subsystem, richiede OutputPipeline>=1): ridistribuisce quei registri
%     ATTRAVERSO la logica combinatoria -> e' cio' che POTREBBE spezzare i 207 livelli del tanh.
%   - ClockRatePipelining (MODEL): registri a clock-rate sfruttando l'oversampling del time-mux.
%  Verdetto make-or-break: se NESSUNA config alza Fmax lasciando il critpath sul tanh (st.dd -> th),
%  il tool NON pipelina il tanh nativo -> F1 si ferma a 9,30 (onesto), senza aprire il core.
  if nargin < 1 || isempty(cfgs)
    % {tag, OutputPipeline, DistributedPipelining, ClockRatePipelining}
    cfgs = { 'baseline', 0, 'off', 'off'
             'op2_dist', 2, 'on',  'off'
             'op4_dist', 4, 'on',  'off'
             'op8_dist', 8, 'on',  'off'
             'op4_crp',  4, 'on',  'on'  };
  end
  md  = 'D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer/matlab';
  addpath(md);
  out = fullfile(md, 'hdl_pipe'); if exist(out,'dir'), rmdir(out,'s'); end; mkdir(out);
  lib = 'snn_champions_lib';
  blk = 'Donatello_ACC_IIDM_M';

  fprintf('\n==== PROBE pipeline tanh (B2.0-2b F1, make-or-break) ====\n');
  for i = 1:size(cfgs,1)
    tag = cfgs{i,1}; op = cfgs{i,2}; dist = cfgs{i,3}; crp = cfgs{i,4};
    bdclose('all');
    load_system(fullfile(md,[lib '.slx'])); set_param(lib,'Lock','off');
    mdl = ['m_pipe_' tag]; if bdIsLoaded(mdl), close_system(mdl,0); end
    new_system(mdl); load_system(mdl);
    dut = [mdl '/DUT'];
    add_block([lib '/' blk], dut);
    try, set_param(dut,'LinkStatus','none'); catch, end     % slink: senza, la chart e' read-only
    vals = {'10','6','2','4'};
    for j = 1:4
      add_block('simulink/Sources/Constant',[mdl '/i' num2str(j)], ...
                'Value',vals{j},'OutDataTypeStr','fixdt(1,32,20)','SampleTime','1');
      add_line(mdl,['i' num2str(j) '/1'],['DUT/' num2str(j)]);
    end
    add_block('built-in/Outport',[mdl '/o'],'Port','1'); add_line(mdl,'DUT/1','o/1');
    set_param(mdl,'Solver','FixedStepDiscrete','FixedStep','1','StopTime','10');

    applied = '';
    if op > 0
      try, hdlset_param(dut,'OutputPipeline',op); applied=[applied sprintf('OP=%d ',op)];
      catch e, fprintf('[%s] OutputPipeline: %s\n',tag,e.message); end
    end
    try, hdlset_param(dut,'DistributedPipelining',dist); applied=[applied 'DIST=' dist ' '];
    catch e, fprintf('[%s] DistributedPipelining: %s\n',tag,e.message); end
    try, hdlset_param(mdl,'ClockRatePipelining',crp); applied=[applied 'CRP=' crp];
    catch e, fprintf('[%s] ClockRatePipelining: %s\n',tag,e.message); end

    tgt = fullfile(out, tag);
    try
      set_param(mdl,'SimulationCommand','update');          % propaga i tipi, rivela errori chart
      makehdl(dut,'TargetLanguage','VHDL','TargetDirectory',tgt,'GenerateHDLTestBench','off');
      v = dir(fullfile(tgt,'**','*.vhd'));
      fprintf('>> %-10s applied[%s]: VHDL OK (%d file) -> %s\n', tag, strtrim(applied), numel(v), tgt);
    catch e
      fprintf('>> %-10s FALLITO: %s\n', tag, regexprep(e.message,'\s+',' '));
    end
    close_system(mdl,0);
  end
  bdclose('all');
  fprintf('\nVHDL per la sintesi OOC in %s (una cartella per config).\n', out);
  fprintf('Sintesi: vivado -mode batch -source scripts/synth_acc_iidm.tcl -tclargs %s\\<tag> %s\\<tag>\\synth <tag>\n', out, out);
end
```

- [ ] **Step 2: Generare solo il baseline e verificare il VHDL**

Run (MATLAB, da `matlab/`):
```matlab
probe_pipe_tanh({'baseline',0,'off','off'})
```
Expected (stdout): riga `>> baseline  applied[DIST=off CRP=off]: VHDL OK (N file) -> ...\hdl_pipe\baseline`, con N≥3 file, **nessun** `FALLITO`. La cartella `matlab/hdl_pipe/baseline/m_pipe_baseline/` contiene `DUT.vhd` + `DualPortRAM_generic.vhd` + `*_pkg.vhd`.

- [ ] **Step 3: Commit**

```bash
cd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer"
git add matlab/probe_pipe_tanh.m matlab/.gitignore
git commit -m "feat: probe_pipe_tanh (B2.0-2b F1, generazione VHDL varianti pipeline)"
```
(Nota: `matlab/hdl_pipe/` è output rigenerabile — assicurarsi che sia in `matlab/.gitignore`; se manca, aggiungere la riga `hdl_pipe/`.)

### Task 0.2: Baseline OOC — il flusso riproduce il numero noto

**Files:**
- Nessuna modifica (solo esecuzione + registrazione).

- [ ] **Step 1: Sintesi OOC del baseline**

Run (PowerShell, dalla working dir):
```powershell
$src = "matlab\hdl_pipe\baseline\m_pipe_baseline"
& "C:\AMDDesignTools\2026.1\Vivado\bin\vivado.bat" -mode batch -source scripts\synth_acc_iidm.tcl `
  -tclargs $src "matlab\hdl_pipe\baseline\synth" baseline
```

- [ ] **Step 2: Leggere RESULT/CRITPATH e verificare che tornino**

Expected (stdout Vivado, ultime righe):
```
RESULT baseline LUT=<~8614> FF=<~2134> DSP=<~71> BRAM=<..> WNS=<..> Fmax=<~9.30>
CRITPATH baseline from=<..> to=<..> logic_levels=<~207> delay=<..>
```
Criterio: `Fmax` in [8,8 ; 9,8] MHz, `LUT` in [8000 ; 9200], `logic_levels` ~207, e l'endpoint del critpath (`to=`) è un segnale del `tanh`/`iidm_final` (contiene `th`/`dd`/`final`). **Se il baseline non torna, fermarsi**: il flusso non è fedele, indagare prima di procedere.

- [ ] **Step 3: Registrare il baseline**

Annotare la riga `RESULT baseline ...` e `CRITPATH baseline ...` in un file di lavoro `matlab/hdl_pipe/RESULTS.txt` (append). Servirà da riferimento per lo sweep. Nessun commit (output rigenerabile).

---

## Fase 1 — Probe make-or-break: HDL Coder pipelina il tanh?

### Task 1.1: Generare le varianti di pipeline

**Files:**
- Nessuna modifica (esecuzione di `probe_pipe_tanh` con lo sweep completo).

- [ ] **Step 1: Generare tutte le config**

Run (MATLAB, da `matlab/`):
```matlab
probe_pipe_tanh()
```
Expected: una riga `>> <tag> ... VHDL OK ...` per ciascuna di `baseline, op2_dist, op4_dist, op8_dist, op4_crp`. Se una config stampa `FALLITO`, annotare il messaggio (è un dato: quella leva non è applicabile alla chart-FSM) e proseguire con le altre.

### Task 1.2: Sintesi OOC dello sweep e verdetto

**Files:**
- Nessuna modifica.

- [ ] **Step 1: Sintetizzare ogni variante generata**

Run (PowerShell): per ogni `<tag>` in `op2_dist, op4_dist, op8_dist, op4_crp` che ha prodotto VHDL:
```powershell
foreach ($tag in @("op2_dist","op4_dist","op8_dist","op4_crp")) {
  $src = "matlab\hdl_pipe\$tag\m_pipe_$tag"
  if (Test-Path $src) {
    & "C:\AMDDesignTools\2026.1\Vivado\bin\vivado.bat" -mode batch -source scripts\synth_acc_iidm.tcl `
      -tclargs $src "matlab\hdl_pipe\$tag\synth" $tag | Tee-Object -Append "matlab\hdl_pipe\RESULTS.txt"
  }
}
```

- [ ] **Step 2: Applicare il criterio make-or-break**

Leggere in `matlab/hdl_pipe/RESULTS.txt` le righe `RESULT <tag> ... Fmax=..` e `CRITPATH <tag> ... logic_levels=.. to=..`. Verdetto:

- **PASS** se esiste ≥1 config con `Fmax` **significativamente > baseline** (soglia pratica: ≥ +8%, cioè ≳ 10,0 MHz) **e** `CRITPATH.to` **non è più** un segnale del `tanh` (l'endpoint si è spostato, es. su `pv`/`R_idx` = SNN→decode) **e** `logic_levels` del tanh è calato. → il tool pipelina il tanh: procedere a **Fase 2** con la config a Fmax massimo (`WINNER`).
- **FAIL** se tutte le config restano `Fmax ≈ 9,30` con `CRITPATH.to` ancora sul `tanh`: HDL Coder **non** ridistribuisce registri dentro il `tanh` nativo. → **Fase 2 saltata**, andare a **Fase 2-bis** (chiusura onesta a 9,30). Non aprire il core: SNN→decode (F2) non ha senso finché il tanh resta il collo.

- [ ] **Step 3: Registrare il verdetto**

Scrivere in `matlab/hdl_pipe/RESULTS.txt` una riga finale `VERDICT F1 = PASS winner=<tag> Fmax=<..>` oppure `VERDICT F1 = FAIL (tanh non pipelinabile via attributi)`. Questo determina il ramo seguente.

---

## Fase 2 — [SE PASS] Applicare la config vincente + non-regressione bit-exact

> Eseguire **solo** se `VERDICT F1 = PASS`. La config `WINNER` è block-level (OutputPipeline+DistributedPipelining) **oppure** model-level (ClockRatePipelining): i due rami differiscono solo nel Task 2.1.

### Task 2.1a: [SE WINNER è block-level] Applicare l'attributo al blocco committato

**Files:**
- Modify: `matlab/build_hdl_variants.m` (sezione `Donatello_ACC_IIDM_M`, dopo l'`add_line(subM, 'IIDM_CTRL/1', 'accel/1');` a riga ~138)

- [ ] **Step 1: Aggiungere l'attributo HDL al blocco M in `build_hdl_variants.m`**

Inserire dopo `add_line(subM, 'IIDM_CTRL/1', 'accel/1');` (sostituendo `<OP>` col valore vincente, es. 4):

```matlab
  % --- SP4-M-FSM #2b: pipeline del tanh (B2.0-2b F1). L'attributo viaggia col blocco (add_block lo
  %     copia): rtl_gen_dut lo rigenera identico per l'harness. Config vincente misurata in OOC
  %     (probe_pipe_tanh -> matlab/hdl_pipe/RESULTS.txt). Bit-exact: lo provano B-1/A-1/PLANT-PAR/B-LOOP.
  hdlset_param(subM, 'OutputPipeline', <OP>);
  hdlset_param(subM, 'DistributedPipelining', 'on');
  fprintf('  applicata pipeline tanh a Donatello_ACC_IIDM_M (OutputPipeline=%d, DistributedPipelining=on)\n', <OP>);
```

- [ ] **Step 2: Ricostruire la libreria e verificare che il blocco porti l'attributo**

Run (MATLAB, da `matlab/`):
```matlab
build_hdl_variants()
```
Then verify:
```matlab
load_system('snn_champions_lib');
hdlget_param('snn_champions_lib/Donatello_ACC_IIDM_M','OutputPipeline')
```
Expected: la build stampa la riga `applicata pipeline tanh ...`; `hdlget_param` ritorna `<OP>` (es. `4`). Andare al Task 2.2.

### Task 2.1b: [SE WINNER è ClockRatePipelining] Applicare il param al generatore RTL

**Files:**
- Modify: `matlab/rtl_gen_dut.m:36-38` (blocco `set_param(mdl,...)` prima di `makehdl`)
- Modify: `matlab/build_hdl_variants.m` (commento di provenienza nella Description del blocco M)

- [ ] **Step 1: Impostare ClockRatePipelining sul modello di generazione**

In `matlab/rtl_gen_dut.m`, subito prima della riga `makehdl(sub, ...)` (riga 38), inserire:

```matlab
  % B2.0-2b F1: ClockRatePipelining e' un param di MODELLO (non viaggia col blocco) -> va impostato qui,
  % altrimenti l'RTL dell'harness non avrebbe la pipeline misurata in OOC (mismatch probe/harness).
  hdlset_param(mdl, 'ClockRatePipelining', 'on');
```
E allineare `probe_pipe_tanh` e la sintesi OOC alla stessa config `op*_crp` (già CRP=on).

- [ ] **Step 2: Rigenerare il DUT Verilog e verificare che compili con CRP**

Run (MATLAB, da `matlab/`):
```matlab
info = rtl_gen_dut('Donatello_ACC_IIDM_M', [], 'Verilog');
```
Expected: `=== Verilog generato: Donatello_ACC_IIDM_M -> ... ===`, nessun errore, `Donatello_ACC_IIDM_M.v` presente. Andare al Task 2.2.

### Task 2.2: B-1 — RTL bit-exact al blocco (gate primario di F1)

**Files:**
- Nessuna modifica (esecuzione del gate esistente, che rigenera il DUT dalla libreria appena ricostruita).

- [ ] **Step 1: Forzare la rigenerazione del DUT Verilog aggiornato**

Run (PowerShell): rimuovere l'hdlsrc stantio così `run_rtl_validate_b` rigenera col nuovo attributo:
```powershell
Remove-Item -Recurse -Force "matlab\hdlsrc_donatello_acc_iidm_m_v" -ErrorAction SilentlyContinue
```

- [ ] **Step 2: Eseguire B-1 sul set ridotto**

Run (MATLAB, da `matlab/`):
```matlab
res = run_rtl_validate_b([1 7 23], 'reduced')
```
Expected: `=== B-1 PASSATO: Donatello_ACC_IIDM_M RTL bit-exact al blocco su 3000/3000 (accel) ===`, `res.nMismatch == 0`. Se `nMismatch > 0`: la pipeline **non** è bit-exact (latch mancante o CRP non bilanciato). **Fermarsi**, annullare l'attributo (git checkout dei file modificati), il verdetto F1 diventa FAIL-bit-exact → Fase 2-bis.

- [ ] **Step 3: Eseguire B-1 sul dataset intero**

Run (MATLAB, da `matlab/`):
```matlab
res = run_rtl_validate_b(1:60, 'full')
```
Expected: `res.nMismatch == 0` su 60000/60000. Questo è il gate bit-exact forte del controllore.

### Task 2.3: A-1 + PLANT-PAR + B-LOOP — non-regressione di contorno

**Files:**
- Nessuna modifica.

- [ ] **Step 1: A-1 (SNN sola, invariata dal fronte F1 — deve restare 0)**

Run (MATLAB, da `matlab/`):
```matlab
run_rtl_validate(1:15)
```
Expected: `=== A-1 PASSATO ... 15000/15000 ===`, 0 mismatch. (F1 tocca l'IIDM, non la SNN: A-1 conferma nessuna regressione accidentale.)

- [ ] **Step 2: PLANT-PAR + B-LOOP (comportamento car-following)**

Run (MATLAB, da `matlab/`):
```matlab
run_plant_par()
run_closed_loop()
```
Expected: PLANT-PAR `dmax=0` su 1800/1800; B-LOOP `dmax=0` su 2400/2400 con gap>0 (nessuna collisione). Se qualcuno fallisce, annullare l'attributo e passare a Fase 2-bis.

### Task 2.4: OOC finale del blocco committato (numero ufficiale di F1)

**Files:**
- Nessuna modifica.

- [ ] **Step 1: OOC del blocco ricostruito (via il probe con la sola config vincente)**

Run (MATLAB, da `matlab/`):
```matlab
probe_pipe_tanh({'final',0,'off','off'})   % 0/off: la pipeline e' gia' nel blocco committato (Task 2.1)
```
Poi (PowerShell):
```powershell
$src = "matlab\hdl_pipe\final\m_pipe_final"
& "C:\AMDDesignTools\2026.1\Vivado\bin\vivado.bat" -mode batch -source scripts\synth_acc_iidm.tcl `
  -tclargs $src "matlab\hdl_pipe\final\synth" final | Tee-Object -Append "matlab\hdl_pipe\RESULTS.txt"
```
Expected: `RESULT final ... Fmax=<WINNER, > 9,30>`, coerente col probe. Questo è l'`Fmax` ufficiale di F1, misurato sul blocco committato (non su una copia scratch con attributi manuali).

> **Nota:** se WINNER è CRP (Task 2.1b), `probe_pipe_tanh({'final',...})` va lanciato con la config `{'final',4,'on','on'}` (o quella vincente) perché CRP non è memorizzato nel blocco — vedi il commento in `rtl_gen_dut.m`. Per il ramo block-level (2.1a) l'attributo è già nel blocco, quindi `0/off/off`.

### Task 2.5: Documentare e committare il checkpoint F1

**Files:**
- Modify: `document/SP4_ACC_IIDM_FAST.md` (nuova sotto-sezione "§2b F1 — pipeline del tanh")
- Modify: `document/HDL_PHASE.md` (§6, aggiornare l'Fmax del controllore)
- Modify: `document/SESSION_RESUME.md` (stato + prossima azione)

- [ ] **Step 1: Scrivere l'esito F1 in `SP4_ACC_IIDM_FAST.md`**

Aggiungere una sotto-sezione con: config vincente (attributo + valore), `Fmax` prima/dopo (9,30 → WINNER), il nuovo critpath (dove si è spostato il collo: atteso SNN→decode), la latenza aggiuntiva misurata (N clock, assorbita da HOLD=500), e la riga "bit-exact: B-1 0/60000, A-1 0/15000, PLANT-PAR/B-LOOP dmax=0". Numeri presi da `matlab/hdl_pipe/RESULTS.txt` e dai gate.

- [ ] **Step 2: Aggiornare `HDL_PHASE.md` §6 e `SESSION_RESUME.md`**

In `HDL_PHASE.md §6`: aggiornare l'`Fmax` del controllore da 9,30 a WINNER, citando la config. In `SESSION_RESUME.md`: stato "F1 chiuso (Fmax WINNER, dmax=0)" e prossima azione ("F2 SNN→decode se il guadagno lo giustifica, altrimenti report finale 2b").

- [ ] **Step 3: Commit del checkpoint F1**

```bash
cd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer"
git add matlab/build_hdl_variants.m matlab/rtl_gen_dut.m document/SP4_ACC_IIDM_FAST.md document/HDL_PHASE.md document/SESSION_RESUME.md
git commit -m "feat: B2.0-2b F1 pipeline tanh su Donatello_ACC_IIDM_M (Fmax 9,30 -> WINNER, dmax=0)"
```
(Committare solo i file effettivamente modificati dal ramo scelto: 2.1a → `build_hdl_variants.m`; 2.1b → `rtl_gen_dut.m` + Description.)

---

## Fase 2-bis — [SE FAIL] Chiusura onesta a 9,30

> Eseguire **solo** se F1 è FAIL (tanh non pipelinabile via attributi, o pipeline non bit-exact).

### Task 2bis.1: Annullare ogni modifica e documentare il tetto

**Files:**
- Modify: `document/SP4_ACC_IIDM_FAST.md` (esito probe F1)
- Modify: `document/SESSION_RESUME.md`

- [ ] **Step 1: Ripristinare i sorgenti eventualmente toccati**

```bash
cd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer"
git checkout -- matlab/build_hdl_variants.m matlab/rtl_gen_dut.m
```
(Il blocco committato resta la versione 9,30 provata.)

- [ ] **Step 2: Documentare il verdetto negativo (è un risultato, non un fallimento)**

In `SP4_ACC_IIDM_FAST.md`: sotto-sezione "§2b F1 — probe pipeline tanh: NEGATIVO", con la tabella `RESULT`/`CRITPATH` dello sweep (da `matlab/hdl_pipe/RESULTS.txt`) e la conclusione: HDL Coder non ridistribuisce registri dentro il `tanh` nativo in una chart-FSM; l'`Fmax` resta 9,30; l'unica strada residua sarebbe reimplementare il `tanh` a mano (rischio §2.1, scartato). In `SESSION_RESUME.md`: "2b chiuso a 9,30 (tanh non pipelinabile); prossimo = report finale B2.0 / 2c".

- [ ] **Step 3: Commit**

```bash
git add document/SP4_ACC_IIDM_FAST.md document/SESSION_RESUME.md matlab/probe_pipe_tanh.m
git commit -m "docs: B2.0-2b F1 probe NEGATIVO (tanh non pipelinabile via attributi HDL); tetto 9,30 confermato"
```
Poi **saltare alla Fase 4** (F2 non ha senso col tanh ancora collo). Fine 2b lato timing.

---

## Fase 3 — [SE F1 PASS e il guadagno giustifica] Probe F2: SNN→decode (nel core)

> **Conditional**: eseguire solo se F1 è PASS **e** il nuovo critpath è ora SNN→decode **e** l'utente conferma di voler aprire il core per il retiming bit-exact (deroga §2 della spec). Riusa la macchina del probe con target il path `pR_idx → pv` (172 liv). Ogni passo che tocca il core è gated da `run_b2_parity_dataset` 0/240000.

### Task 3.1: Probe OOC del fronte SNN→decode

**Files:**
- Nessuna modifica (riusa `probe_pipe_tanh` con `OutputPipeline` maggiore, per dare registri anche al secondo stadio).

- [ ] **Step 1: Sweep con più registri di output**

Run (MATLAB, da `matlab/`):
```matlab
probe_pipe_tanh({ 'f2_op6', 6, 'on', 'off'
                  'f2_op8', 8, 'on', 'off'
                  'f2_crp', 6, 'on', 'on' })
```
Poi sintetizzare ciascuna (PowerShell, come Task 1.2 Step 1) e leggere `RESULT`/`CRITPATH`.

- [ ] **Step 2: Verdetto F2**

- **PASS** se ≥1 config alza `Fmax` oltre il WINNER di F1 (verso ~11,65) **e** `CRITPATH.to` lascia il path SNN→decode. → procedere al Task 3.2 con la nuova config `WINNER2`.
- **FAIL** se l'`Fmax` non sale oltre F1 o il critpath resta su SNN→decode: il retiming del core non rende. → **non** applicare, documentare (come Fase 2-bis ma per F2), il core resta intatto, andare a Fase 4.

### Task 3.2: [SE F2 PASS] Applicare + non-regressione PESANTE (parity del core)

**Files:**
- Modify: `matlab/build_hdl_variants.m` (o `rtl_gen_dut.m` se CRP) — aggiornare l'attributo di pipeline a `WINNER2`.

- [ ] **Step 1: Applicare `WINNER2` (stesso meccanismo del Task 2.1a/2.1b) e ricostruire**

Aggiornare il valore `<OP>` (o CRP) al vincente F2 e rieseguire `build_hdl_variants()` (o `rtl_gen_dut(...)`).

- [ ] **Step 2: Gate del core — `run_b2_parity_dataset` (0/240000)**

Run (MATLAB, da `matlab/`):
```matlab
run_b2_parity_dataset('Donatello')
```
Expected: `0/240000` disallineamenti (SNN bit-exact end-to-end). **Questo è il gate invalicabile del core.** Se ≠ 0/240000: il retiming ha rotto la rete → `git checkout` immediato dei file, F2 = FAIL-bit-exact, core ripristinato.

- [ ] **Step 3: Ripetere B-1 (full) + A-1 + PLANT-PAR + B-LOOP**

Run (MATLAB, da `matlab/`):
```matlab
run_rtl_validate_b(1:60, 'full')   % 0/60000
run_rtl_validate(1:15)             % 0/15000
run_plant_par()                    % dmax=0
run_closed_loop()                  % dmax=0, gap>0
```
Expected: tutti a dmax=0. Poi OOC finale (come Task 2.4) per l'`Fmax` ufficiale di F2.

- [ ] **Step 4: Documentare e committare il checkpoint F2**

Aggiornare `SP4_ACC_IIDM_FAST.md` (§2b F2), `HDL_PHASE.md §6`, `SESSION_RESUME.md` con l'`Fmax` finale e la riga "core bit-exact: run_b2_parity_dataset 0/240000". Commit:
```bash
git add matlab/build_hdl_variants.m matlab/rtl_gen_dut.m document/SP4_ACC_IIDM_FAST.md document/HDL_PHASE.md document/SESSION_RESUME.md
git commit -m "feat: B2.0-2b F2 pipeline SNN->decode (Fmax -> WINNER2, core bit-exact 0/240000)"
```

---

## Fase 4 — Chiusura 2b

### Task 4.1: Aggiornare la memoria e il resume

**Files:**
- Modify: `document/SESSION_RESUME.md`
- Modify (memoria utente): `.../memory/cf-fsnn-b15-champion-library-expansion.md` + `MEMORY.md` (una riga)

- [ ] **Step 1: Consolidare lo stato di 2b in `SESSION_RESUME.md`**

Riga di stato finale: F1 (esito + Fmax), F2 (esito + Fmax o "non aperto/negativo"), gate verdi elencati, prossima azione ("report finale B2.0" oppure "2c studio quantizzazione"). Aggiornare l'`Fmax` del controllore ovunque citato 9,30.

- [ ] **Step 2: Aggiornare la memoria di progetto**

In `cf-fsnn-b15-champion-library-expansion.md` aggiungere l'esito 2b (Fmax raggiunto, bit-exact, se il core è stato aperto). Aggiornare la riga corrispondente in `MEMORY.md`. Convertire date relative in assolute.

- [ ] **Step 3: Commit finale**

```bash
cd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer"
git add document/SESSION_RESUME.md
git commit -m "docs: chiusura B2.0-2b (timing), stato e prossima azione aggiornati"
```
(La memoria utente è fuori dal repo: si salva con Write, non con git.)

---

## Self-review (svolta in fase di stesura)

**Spec coverage:** §1 obiettivo → Fase 0-2/verdetto; §2 vincolo+deroga core → gate B-1/A-1/PLANT/B-LOOP (F1) e `run_b2_parity_dataset` (F2, Task 3.2 Step 2); §3 collo tanh → Task 0.2/1.2 (critpath); §4 approccio A + §9 dataflow → probe su attributi, chart sola nel DUT (Task 0.1); §5 probe make-or-break → Fase 1; §6 fasi condizionate → Fase 2/2-bis/3; §7 non-regressione + latenza<HOLD → Task 2.2-2.3 (tb assorbe, confermato); §8 rischi (non ingrana → 2-bis; core → parity) → coperti; §9 criteri di successo → OOC finale (2.4/3.3) + gate. Nessun requisito scoperto.

**Placeholder scan:** i `<OP>`/`<tag>`/`WINNER`/`WINNER2` sono valori **misurati dal probe** e sostituiti in fase di esecuzione (non placeholder di design ma parametri data-driven, per costruzione del make-or-break); ogni step ha codice/comando concreto ed expected. Nessun "TBD/handle edge cases".

**Type/nome consistency:** nomi verificati sui sorgenti reali — `probe_pipe_tanh`, `synth_acc_iidm.tcl` (top `DUT`, RESULT/CRITPATH), `run_rtl_validate_b([...],'reduced'|'full')`, `run_rtl_validate`, `run_plant_par`, `run_closed_loop`, `run_b2_parity_dataset('Donatello')`, `rtl_gen_dut('Donatello_ACC_IIDM_M',[],'Verilog')`, `hdlset_param/hdlget_param`, blocco `Donatello_ACC_IIDM_M` (Subsystem con chart `IIDM_CTRL`), tb `Donatello_ACC_IIDM_M(.clk,.reset,.clk_enable,.s,.v,.dv,.v_l,.accel,.ce_out)`, `HOLD=500`. Coerenti.
