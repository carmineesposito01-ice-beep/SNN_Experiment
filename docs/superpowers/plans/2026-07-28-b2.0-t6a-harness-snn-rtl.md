# Fase B2.0 · T6a — Harness_SNN: validazione RTL + metriche (Donatello_Tier@BALANCED) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** provare in xsim che il **VHDL generato** di `Donatello_Tier@BALANCED` riproduce **bit-exact il blocco**
sul dataset dei 60 (gate su subset diversificato + full-60), con cancelli provati sensibili, e riportare
l'accuratezza di stima dei 5 param inquadrata con l'identificabilità. (L'hardware — utilizzo post-route, power,
bitstream — è il piano **T6b**.)

**Architecture:** MATLAB genera `stim.mem` (i `val`, `fixdt(1,32,20)`) e `gold.mem` (i 5 param **dal blocco stesso**
via `tier_block_params`, l'oracolo) → un TB VHDL pilota il DUT in xsim → self-checking → cancelli + metriche. Riusa
il core M1 (`rtl_gen_dut` esteso, pattern `drive`, formati `.mem`). **Golden-da-blocco** sul subset; per il full-60
un probe di performance decide (background-Simulink vs estrazione-MEX).

**Struttura cartelle (Fase B2.0 — VINCOLANTE, `FaseB2.0/README.md`):**
- **`matlab/`** = golden + gen HDL (i golden «vivono in ../matlab/, riusati non copiati»): `rtl_gen_dut`, `tier_block_params`.
- **`FaseB2.0/common/`** = utilità RTL **condivise/generiche** (riusate da T7): `rtl_write_vectors.m`, `rtl_run_xsim.sh`.
- **`FaseB2.0/Harness_SNN/`** = **specifico del banco**: `tb`, `run`/orchestratore, exporter, metriche, probe, README, `results/`.
- **work-dir xsim = `D:/zbd_tier`** (ROOT corta **SENZA spazi**: il path del repo contiene `1.Reti Neurali` → xsim/glob romperebbe, `FaseB2.0/README.md` riga 55). Gli `.mem` e gli artefatti xsim vivono **lì** (fuori dal repo), i log tenuti si copiano in `Harness_SNN/results/`.

**Tech Stack:** MATLAB R2026a headless (`"C:\Program Files\MATLAB\R2026a\bin\matlab.exe" -batch`) · HDL Coder
`makehdl` · Vivado 2026.1 `xvhdl/xvlog/xelab/xsim` (`C:\AMDDesignTools\2026.1\Vivado\bin`) · Git-Bash per il runner.

**Riferimenti codice (leggere prima):** `matlab/rtl_gen_dut.m` (gen VHDL + entità + `compile_order.txt`),
`matlab/run_block_traj_test.m` (helper `drive` + latenza + campionamento fine-hold), `matlab/build_tier_configurable.m`
(mask `TIER` default `BALANCED`/`NFRAC 13`, `VariantActivationTime='update diagram'`), `matlab/rtl_export_vectors.m`
(formati `.mem`), `matlab/axi/champion/tb_champion_stream.v` (pattern TB), `matlab/axi/build/phase_b/gen_saif_b2.sh`
(pattern work-dir corta), spec `docs/superpowers/specs/2026-07-28-b2.0-t6-harness-snn-design.md`.

**Convenzioni (vincolanti):** commit conventional **senza** `Co-Authored-By`; stage **esplicito** (mai `git commit -am`;
i `*.mexw64` sono tracciati); non toccare `matlab/closed_loop_demo.slx`/`matlab/slblocks.m`. Verifica **sul dataset**
(quanti/quanti); un cancello che non può fallire non è un cancello (assert + prova di sensibilità). **Core SNN
congelato; VHDL mai a mano.** DUT in **VHDL** (la SNN non ha divisore). Gli script `FaseB2.0/**` fanno
`addpath('../../matlab')` (golden/sorgenti) e `addpath('../common')` (utilità).

---

## File Structure

| File | Dove | Responsabilità |
|---|---|---|
| `matlab/rtl_gen_dut.m` (**modify**) | matlab/ | +4° arg `maskParams` per forzare la variante prima di `makehdl` |
| `matlab/hdlsrc_donatello_tier/` (gen, **gitignore**) | matlab/ | VHDL del Tier@BAL + `compile_order.txt` |
| `matlab/tier_block_params.m` (create) | matlab/ | **oracolo**: pilota `Donatello_Tier@BAL` in Simulink → 5 param `Q7.13` + latenza misurata |
| `FaseB2.0/common/rtl_write_vectors.m` (create) | common/ | writer `.mem` **generico** (stim/gold fi → hex storedInteger) |
| `FaseB2.0/common/rtl_run_xsim.sh` (create) | common/ | runner xsim **generico**: copia HDL+TB+.mem in ROOT corta, compila, gira |
| `FaseB2.0/Harness_SNN/tier_export_vectors.m` (create) | Harness_SNN/ | stim+gold `.mem` in ROOT, dal golden-da-blocco |
| `FaseB2.0/Harness_SNN/test_tier_export.m` (create) | Harness_SNN/ | round-trip test dell'exporter |
| `FaseB2.0/Harness_SNN/tb_tier_stream.v` (create) | Harness_SNN/ | TB VHDL: campiona i 5 param a fine `HOLD`, misura `LAT`, stampa `RTLRES` |
| `FaseB2.0/Harness_SNN/run_rtl_validate_tier.m` (create) | Harness_SNN/ | orchestratore: **T6-EXACT** + sensibilità + **LAT** + **PORT-TYPE**, `mode ∈ {reduced,full}` |
| `FaseB2.0/Harness_SNN/probe_golden_cost.m` (create) | Harness_SNN/ | misura il costo del golden → decide il full-60 |
| `FaseB2.0/Harness_SNN/tier_rtl_metrics.m` (create) | Harness_SNN/ | accuratezza stima 5 param vs `gt_params` (con identificabilità) |
| `FaseB2.0/Harness_SNN/README.md` (create) | Harness_SNN/ | comandi + mappa prodotti |
| `matlab/.gitignore` (**modify**) | matlab/ | `hdlsrc_donatello_tier/` |

**Subset diversificato:** le 8 combinazioni scenario×profilo di `test_dataset.mat` (1 traj/combinazione) + estremi di
`dv` + ≥1 con cut-in. Indici calcolati da `subset_diverse` (Task 6), non hardcoded.

---

## Task 1: Estendere `rtl_gen_dut` alla variante + generare il VHDL del Tier@BAL

**Files:** Modify `matlab/rtl_gen_dut.m`, `matlab/.gitignore` · Gen `matlab/hdlsrc_donatello_tier/`

- [ ] **Step 1: gitignore**

In fondo a `matlab/.gitignore`:
```
# Fase B2.0 T6 — VHDL generato del Tier (rigenerabile via rtl_gen_dut)
matlab/hdlsrc_donatello_tier/
```
(Gli `.mem`/artefatti xsim vivono in `D:/zbd_tier`, **fuori dal repo** → niente gitignore per loro.)

- [ ] **Step 2: aggiungere il 4° argomento `maskParams`**

In `matlab/rtl_gen_dut.m`, riga 1:
```matlab
function info = rtl_gen_dut(blockName, outdir, lang)
```
→
```matlab
function info = rtl_gen_dut(blockName, outdir, lang, maskParams)
%  maskParams (opz.): cell {name,value,...} applicato al blocco copiato PRIMA di makehdl. Per un Variant
%  Subsystem (es. Donatello_Tier) seleziona la variante: {'TIER','BALANCED','NFRAC','13'}. Il blocco ha gia'
%  VariantActivationTime='update diagram' -> makehdl genera SOLO la variante scelta. Default {} = invariato.
```
Accanto agli altri default aggiungi `if nargin < 4, maskParams = {}; end`. Dopo `add_block([lib '/' blockName], sub);`:
```matlab
  if ~isempty(maskParams)
    set_param(sub, maskParams{:});   % seleziona la variante (es. TIER=BALANCED, NFRAC=13)
  end
```

- [ ] **Step 3: generare il VHDL del Tier@BAL + leggere l'entità (il "test")**

Run:
```bash
"C:/Program Files/MATLAB/R2026a/bin/matlab.exe" -sd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer/matlab" -batch "info=rtl_gen_dut('Donatello_Tier',[],'VHDL',{'TIER','BALANCED','NFRAC','13'}); fprintf('nIn=%d nOut=%d\n',info.nIn,info.nOut)"
```
Expected: `=== VHDL generato: Donatello_Tier -> ...hdlsrc_donatello_tier... ===`, l'assert `DualPortRAM` passa,
la ENTITY grezza mostra 4 ingressi (`s,v,dv,v_l` 32b) + 5 uscite (`v0,T,s0,a,b` 21b) + `clk/reset/clk_enable/ce_out`,
`nIn=4 nOut=5`. **Trascrivi i nomi porta** (per il TB, Task 5). Se genera più di un top → la variante non è stata
forzata: verifica `TIER`/`NFRAC` e `VariantActivationTime`; **non** proseguire.

- [ ] **Step 4: commit**
```bash
cd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer"
git add matlab/rtl_gen_dut.m matlab/.gitignore
git commit -m "feat(b2.0-t6): rtl_gen_dut accetta maskParams per forzare la variante (Tier@BAL)"
```

---

## Task 2: L'oracolo `matlab/tier_block_params.m` + gate edge-trigger

**Files:** Create `matlab/tier_block_params.m`

- [ ] **Step 1: scrivere `tier_block_params.m`** (in `matlab/`, accanto agli altri golden)

```matlab
function [P, lat] = tier_block_params(trajList, hold, nfrac)
%TIER_BLOCK_PARAMS  Oracolo T6: gira Donatello_Tier@BALANCED (nfrac Base 13) in Simulink su ogni traiettoria
%  di test_dataset.mat, campiona i 5 param [v0 T s0 a b] a fine control-step. Fedele al blocco PER COSTRUZIONE.
%  P{i} = N_i x 5 (double); lat = latenza misurata. hold >= latenza (~364 BAL splitpipe); default 500 (== TB).
%  nfrac = bit frazionari ingresso (default 20 = bit-exact; <20 = controllo negativo, HDL_PHASE §3.1.3).
  if nargin<2 || isempty(hold),  hold  = 500; end
  if nargin<3 || isempty(nfrac), nfrac = 20;  end
  here = fileparts(mfilename('fullpath'));
  ds = load(fullfile(here,'test_dataset.mat')); tr = ds.trajectories;
  v1 = double(tr{trajList(1)}.val); v1 = v1(:,1);
  Plog = drive_tier(v1*ones(1,3), 700, 700, nfrac);           % ingresso costante -> 1 sola inferenza
  chg  = find(any(abs(diff(Plog,1,1))>0, 2));
  assert(~isempty(chg), 'il blocco non produce output (FSM ferma?)');
  assert(numel(chg)==1, 'ingresso costante -> %d inferenze: non e'' edge-triggered', numel(chg));
  lat = chg(1);
  assert(hold >= lat, 'hold=%d < latenza=%d', hold, lat);
  fprintf('tier_block_params: latenza BAL misurata = %d clock (edge-trigger OK)\n', lat);
  P = cell(numel(trajList),1);
  for i = 1:numel(trajList)
    val = double(tr{trajList(i)}.val); K = size(val,2);
    Pall = drive_tier(val, hold, K*hold+20, nfrac);
    idx  = hold*(0:K-1).' + lat + 1; idx = idx(idx <= size(Pall,1));
    assert(numel(idx)==K, 'traj %d: campionati %d/%d control-step', trajList(i), numel(idx), K);
    P{i} = Pall(idx,:);
  end
end

function P = drive_tier(seq, hold, stopT, nfrac)
% pilota Donatello_Tier@BALANCED@13 con la sequenza fisica seq (4 x K), ogni colonna tenuta `hold` clock.
  if nargin<3||isempty(stopT), stopT = hold; end
  here = fileparts(mfilename('fullpath'));
  K = size(seq,2);
  assignin('base','stimTS_tier', [(0:K-1).'*hold, seq.']);
  lib = 'snn_champions_lib'; if ~bdIsLoaded(lib), load_system(fullfile(here,[lib '.slx'])); end
  mdl = 'tier_oracle_mdl'; if bdIsLoaded(mdl), close_system(mdl,0); end
  new_system(mdl); load_system(mdl);
  add_block([lib '/Donatello_Tier'], [mdl '/DUT']);
  set_param([mdl '/DUT'], 'TIER','BALANCED', 'NFRAC','13');    % esplicito: niente default impliciti
  add_block('simulink/Sources/From Workspace', [mdl '/src'], 'VariableName','stimTS_tier', ...
            'SampleTime','1','Interpolate','off','OutputAfterFinalValue','Holding final value');
  add_block('simulink/Signal Routing/Demux', [mdl '/dm'], 'Outputs','4'); add_line(mdl,'src/1','dm/1');
  for j=1:4
    add_block('simulink/Signal Attributes/Data Type Conversion', [mdl '/c' num2str(j)], ...
              'OutDataTypeStr', sprintf('fixdt(1,32,%d)', nfrac));
    add_line(mdl, ['dm/' num2str(j)], ['c' num2str(j) '/1']);
    add_line(mdl, ['c' num2str(j) '/1'], ['DUT/' num2str(j)]);
  end
  add_block('simulink/Signal Routing/Mux', [mdl '/mx'], 'Inputs','5');
  for j=1:5, add_line(mdl, ['DUT/' num2str(j)], ['mx/' num2str(j)]); end
  add_block('simulink/Sinks/To Workspace', [mdl '/Pw'], 'VariableName','Pw','SaveFormat','Array');
  add_line(mdl,'mx/1','Pw/1');
  set_param(mdl,'Solver','FixedStepDiscrete','FixedStep','1','StopTime',num2str(stopT),'SaveOutput','off');
  so = sim(mdl); P = double(so.get('Pw')); close_system(mdl,0);
end
```

- [ ] **Step 2: gate edge-trigger + latenza (deve PASSARE)**

Run:
```bash
"C:/Program Files/MATLAB/R2026a/bin/matlab.exe" -sd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer/matlab" -batch "[P,lat]=tier_block_params(1,500); fprintf('lat=%d size=%dx%d\n', lat, size(P{1},1), size(P{1},2))"
```
Expected: `latenza BAL misurata = 364 clock` (costante, `< 500`), `size = 1000x5`. Se `numel(chg)~=1` → non edge-triggered: **fermati e indaga** (HDL_PHASE §3.1.4).

- [ ] **Step 3: commit**
```bash
git add matlab/tier_block_params.m
git commit -m "feat(b2.0-t6): tier_block_params - oracolo (blocco Tier@BAL in Simulink) + gate edge-trigger"
```

---

## Task 3: Utilità condivise `common/` (writer `.mem` + runner xsim)

**Files:** Create `FaseB2.0/common/rtl_write_vectors.m`, `FaseB2.0/common/rtl_run_xsim.sh`

- [ ] **Step 1: `rtl_write_vectors.m`** (writer generico, riusato da T7)

```matlab
function rtl_write_vectors(outdir, tag, stim, stimW, gold, goldW)
%RTL_WRITE_VECTORS  Scrive stim_<tag>.mem e gold_<tag>.mem da matrici fi (colonna = control-step). Generico T6/T7.
%  stim : nS x K fi (es. 4 x K, val fixdt(1,32,20)) -> stimW bit (nhex = stimW/4)
%  gold : nG x K fi (es. 5 x K param Q7.13 / 1 x K accel Q4.8) -> goldW bit
%  Valore = STORED INTEGER (compl. a 2) mascherato via typecast int32->uint32 (uint32 di negativo SATURA -> falsi mismatch).
  if ~exist(outdir,'dir'), mkdir(outdir); end
  K = size(stim,2); nS = size(stim,1); nG = size(gold,1);
  assert(size(gold,2)==K, 'stim/gold: K diverso (%d vs %d)', K, size(gold,2));
  fs = fopen(fullfile(outdir,['stim_' tag '.mem']),'w');
  fg = fopen(fullfile(outdir,['gold_' tag '.mem']),'w');
  fmtS = sprintf('%%0%dX\n', ceil(stimW/4)); fmtG = sprintf('%%0%dX\n', ceil(goldW/4));
  for k = 1:K
    for i=1:nS, fprintf(fs, fmtS, mask_hex(stim(i,k), stimW)); end
    for i=1:nG, fprintf(fg, fmtG, mask_hex(gold(i,k), goldW)); end
  end
  fclose(fs); fclose(fg);
  fprintf('rtl_write_vectors: %d control-step (stim %dx, gold %dx) -> %s\n', K, nS, nG, outdir);
end
function u = mask_hex(x_fi, nbit)
  u = bitand(typecast(int32(storedInteger(x_fi)),'uint32'), uint32(2^nbit-1));
end
```

- [ ] **Step 2: `rtl_run_xsim.sh`** (runner generico, work-dir corta)

```bash
#!/usr/bin/env bash
# Runner xsim generico (T6/T7): copia HDL + TB + .mem in ROOT corta (no spazi) e gira xsim.
# args: ROOT HDLSRC_DIR TB_FILE K HOLD STIM_MEM GOLD_MEM   (STIM/GOLD gia' in ROOT; HDLSRC/TB possono avere spazi)
set -e
VIV="C:/AMDDesignTools/2026.1/Vivado/bin"
ROOT="$1"; HDLSRC="$2"; TB="$3"; K="$4"; HOLD="$5"; STIM="$6"; GOLD="$7"
mkdir -p "$ROOT"; cd "$ROOT"
rm -rf xsim.dir *.jou *.log *.pb hdl; mkdir hdl
cp "$HDLSRC"/*.vhd hdl/
cp "$HDLSRC/compile_order.txt" hdl/
TBN="$(basename "$TB")"; cp "$TB" "./$TBN"
# macro via file (il -d NAME=val perde '=' in Git-Bash -> .bat)
printf '`define KVAL %s\n`define HOLD %s\n`define STIMF "%s"\n`define GOLDF "%s"\n' "$K" "$HOLD" "$STIM" "$GOLD" > tier_params.vh
mapfile -t ORD < hdl/compile_order.txt
for f in "${ORD[@]}"; do "$VIV/xvhdl.bat" "hdl/$f" >/dev/null; done
"$VIV/xvlog.bat" -i . "$TBN" >/dev/null
"$VIV/xelab.bat" -debug off "${TBN%.v}" -s snap >/dev/null
"$VIV/xsim.bat" snap -R
```

- [ ] **Step 3: commit**
```bash
git add FaseB2.0/common/rtl_write_vectors.m FaseB2.0/common/rtl_run_xsim.sh
git commit -m "feat(b2.0-t6): utilita condivise common/ - writer .mem + runner xsim (work-dir corta)"
```

---

## Task 4: Exporter `tier_export_vectors` (Harness_SNN) + round-trip test

**Files:** Create `FaseB2.0/Harness_SNN/tier_export_vectors.m`, `FaseB2.0/Harness_SNN/test_tier_export.m`

- [ ] **Step 1: round-trip test (RED)**

`FaseB2.0/Harness_SNN/test_tier_export.m`:
```matlab
function test_tier_export()
%TEST_TIER_EXPORT  Le .mem rileggono ESATTAMENTE i valori dell'oracolo? (round-trip storedInteger)
  here = fileparts(mfilename('fullpath')); addpath(fullfile(here,'..','..','matlab'));
  outdir = fullfile(tempdir,'tier_export_test'); if exist(outdir,'dir'), rmdir(outdir,'s'); end; mkdir(outdir);
  trajList = [1 33];
  tier_export_vectors(trajList, 'rt', outdir);
  m = load(fullfile(outdir,'meta_rt.mat'));
  stim = readmem(fullfile(outdir,'stim_rt.mem')); gold = readmem(fullfile(outdir,'gold_rt.mem'));
  assert(numel(stim)==m.K*4, 'stim: %d parole, atteso %d', numel(stim), m.K*4);
  assert(numel(gold)==m.K*5, 'gold: %d parole, atteso %d', numel(gold), m.K*5);
  P = tier_block_params(trajList(1), 500); p = P{1}(1,:); Tp = numerictype(1,21,13);
  for i=1:5
    exp_i = bitand(typecast(int32(storedInteger(fi(p(i),Tp))),'uint32'), uint32(2^21-1));
    assert(gold(i)==exp_i, 'gold param %d: file=%X atteso=%X', i, gold(i), exp_i);
  end
  fprintf('=== TEST_TIER_EXPORT PASSATO: %d control-step, round-trip bit-exact ===\n', m.K);
end
function w = readmem(f)
  t = strsplit(strtrim(fileread(f))); w = uint32(hex2dec(t(~cellfun(@isempty,t))));
end
```
Run: `"C:/Program Files/MATLAB/R2026a/bin/matlab.exe" -sd "<Harness_SNN>" -batch "test_tier_export"`
Expected: **FAIL** con `Unrecognized function 'tier_export_vectors'`.

- [ ] **Step 2: `tier_export_vectors.m`**

```matlab
function tier_export_vectors(trajList, tag, outdir)
%TIER_EXPORT_VECTORS  stim/gold .mem + meta per l'Harness_SNN, dal golden FEDELE AL BLOCCO (tier_block_params).
%  outdir = ROOT corta (D:/zbd_tier). stim = 4 val (fixdt(1,32,20), 8 hex); gold = 5 param Q7.13 (21b, 6 hex).
  here  = fileparts(mfilename('fullpath'));
  addpath(fullfile(here,'..','..','matlab'));   % tier_block_params, test_dataset
  addpath(fullfile(here,'..','common'));         % rtl_write_vectors
  ds = load(fullfile(here,'..','..','matlab','test_dataset.mat')); tr = ds.trajectories;
  Tp = numerictype(1,21,13); HOLD = 500;
  P = tier_block_params(trajList, HOLD);          % golden dall'oracolo (matlab/)
  stimC = {}; goldC = {};
  for i = 1:numel(trajList)
    stimC{end+1} = fi(double(tr{trajList(i)}.val),1,32,20);  % 4 x Ni
    goldC{end+1} = fi(P{i}.', Tp);                            % 5 x Ni
  end
  stim = [stimC{:}]; gold = [goldC{:}];
  rtl_write_vectors(outdir, tag, stim, 32, gold, 21);
  K = size(stim,2);
  save(fullfile(outdir,['meta_' tag '.mat']), 'K', 'trajList');
  fprintf('tier_export_vectors: %d control-step su %d traiettorie -> %s\n', K, numel(trajList), outdir);
end
```

- [ ] **Step 3: round-trip test (GREEN)**

Run: `matlab -sd "<Harness_SNN>" -batch "test_tier_export"`
Expected: **PASS** — `=== TEST_TIER_EXPORT PASSATO … round-trip bit-exact ===`.

- [ ] **Step 4: commit**
```bash
git add FaseB2.0/Harness_SNN/tier_export_vectors.m FaseB2.0/Harness_SNN/test_tier_export.m
git commit -m "feat(b2.0-t6): tier_export_vectors (golden-da-blocco -> .mem in ROOT) + round-trip test"
```

---

## Task 5: Testbench VHDL (Harness_SNN) + smoke xsim in ROOT

**Files:** Create `FaseB2.0/Harness_SNN/tb_tier_stream.v`

- [ ] **Step 1: `tb_tier_stream.v`** ⚠️ conferma i nomi porta con la Task 1

```verilog
`timescale 1ns/1ps
`include "tier_params.vh"
// Pilota Donatello_Tier (VHDL) con gli stim, campiona i 5 param a fine control-step vs gold; misura LAT.
module tb_tier_stream;
  localparam integer K    = `KVAL;
  localparam integer HOLD = `HOLD;      // >= latenza RTL (~364); campiona a fine control-step
  reg clk = 0, reset = 0, clk_enable = 1;
  reg  signed [31:0] s, v, dv, v_l;
  wire signed [20:0] v0, T, s0, a, b;   // Q7.13, 21b (confermare nomi da Task 1)
  wire ce_out;
  reg  [31:0] stim [0:K*4-1];
  reg  [20:0] gold [0:K*5-1];
  reg  signed [20:0] pw [0:4];
  integer k, i, nmis, lat; reg signed [20:0] prev0;

  Donatello_Tier dut (.clk(clk), .reset(reset), .clk_enable(clk_enable),
     .s(s), .v(v), .dv(dv), .v_l(v_l), .v0(v0), .T(T), .s0(s0), .a(a), .b(b), .ce_out(ce_out));

  always #62.5 clk = ~clk;              // 8 MHz

  initial begin
    $readmemh(`STIMF, stim); $readmemh(`GOLDF, gold);
    s = stim[0]; v = stim[1]; dv = stim[2]; v_l = stim[3];   // 1o control-step PRIMA del de-assert reset
    reset = 1; repeat (8) @(posedge clk); reset = 0;
    prev0 = v0; lat = 0;                                     // misura LAT: 1o clock in cui v0 cambia
    while (v0 === prev0 && lat < HOLD) begin @(posedge clk); lat = lat + 1; end
    $display("LAT_RTL %0d", lat);
    repeat (HOLD - lat) @(posedge clk);
    nmis = 0; pw[0]=v0; pw[1]=T; pw[2]=s0; pw[3]=a; pw[4]=b;
    for (i=0;i<5;i=i+1) if (pw[i] !== gold[i]) nmis = nmis + 1;
    for (k = 1; k < K; k = k + 1) begin
      s = stim[k*4+0]; v = stim[k*4+1]; dv = stim[k*4+2]; v_l = stim[k*4+3];
      repeat (HOLD) @(posedge clk);
      pw[0]=v0; pw[1]=T; pw[2]=s0; pw[3]=a; pw[4]=b;
      for (i=0;i<5;i=i+1) if (pw[i] !== gold[k*5+i]) nmis = nmis + 1;
    end
    $display("RTLRES nMismatch=%0d n=%0d", nmis, K*5);
    $finish;
  end
endmodule
```

- [ ] **Step 2: smoke — genera VHDL + 1 traj + gira xsim in ROOT**

```bash
"C:/Program Files/MATLAB/R2026a/bin/matlab.exe" -sd "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer/matlab" -batch "rtl_gen_dut('Donatello_Tier',[],'VHDL',{'TIER','BALANCED','NFRAC','13'}); addpath('../FaseB2.0/Harness_SNN'); tier_export_vectors(1,'red','D:/zbd_tier'); m=load('D:/zbd_tier/meta_red.mat'); fprintf('K=%d\n',m.K)"
HD="D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer/matlab/hdlsrc_donatello_tier"
TB="D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer/FaseB2.0/Harness_SNN/tb_tier_stream.v"
bash "D:/Project_MBSE/1.Reti Neurali/Rete_SNN_Test/CF_FSNN/.worktrees/Simulink_Importer/FaseB2.0/common/rtl_run_xsim.sh" "D:/zbd_tier" "$HD" "$TB" <K> 500 stim_red.mem gold_red.mem
```
Expected: xsim elabora senza errori, stampa `LAT_RTL <~364>` poi `RTLRES nMismatch=<n> n=<K*5>`.

- [ ] **Step 3: commit**
```bash
git add FaseB2.0/Harness_SNN/tb_tier_stream.v
git commit -m "feat(b2.0-t6): TB Harness_SNN (Donatello_Tier, 5 param vs golden, LAT) + smoke xsim in ROOT"
```

---

## Task 6: Cancello **T6-EXACT** su subset diversificato + sensibilità

**Files:** Create `FaseB2.0/Harness_SNN/run_rtl_validate_tier.m`

- [ ] **Step 1: orchestratore + selezione subset**

```matlab
function res = run_rtl_validate_tier(mode, trajList)
%RUN_RTL_VALIDATE_TIER  T6-EXACT: genera vettori dal golden-da-blocco (in ROOT), gira xsim, assert. mode
%  'reduced' (subset diversificato) o 'full' (1:60). Cancelli: T6-EXACT (nMismatch==0), LAT (Task 7).
  here   = fileparts(mfilename('fullpath'));
  mroot  = fullfile(here,'..','..','matlab'); addpath(mroot); addpath(fullfile(here,'..','common'));
  ROOT   = 'D:/zbd_tier';
  hdlsrc = fullfile(mroot,'hdlsrc_donatello_tier');
  tb     = fullfile(here,'tb_tier_stream.v');
  runner = fullfile(here,'..','common','rtl_run_xsim.sh');
  if nargin<1||isempty(mode), mode='reduced'; end
  if nargin<2||isempty(trajList)
    if strcmp(mode,'full'), trajList = 1:60; else, trajList = subset_diverse(mroot); end
  end
  if ~exist(fullfile(hdlsrc,'Donatello_Tier.vhd'),'file')
    rtl_gen_dut('Donatello_Tier', hdlsrc, 'VHDL', {'TIER','BALANCED','NFRAC','13'});
  end
  tag = mode; HOLD = 500;
  tier_export_vectors(trajList, tag, ROOT);
  m = load(fullfile(ROOT,['meta_' tag '.mat']));
  cmd = sprintf('bash "%s" "%s" "%s" "%s" %d %d stim_%s.mem gold_%s.mem', ...
                runner, ROOT, hdlsrc, tb, m.K, HOLD, tag, tag);
  [~, out] = system(cmd);
  tok = regexp(out, 'RTLRES nMismatch=(\d+) n=(\d+)', 'tokens', 'once');
  assert(~isempty(tok), 'xsim non ha prodotto RTLRES. Output:\n%s', out);
  res = struct('nMismatch',str2double(tok{1}), 'n',str2double(tok{2}), 'K',m.K, 'traj',numel(trajList));
  lt = regexp(out,'LAT_RTL (\d+)','tokens','once'); res.lat = str2double(lt{1});
  fprintf('T6-EXACT [%s]: nMismatch = %d / %d  (%d traj x %d control-step x 5 param; lat=%d)\n', ...
          mode, res.nMismatch, res.n, res.traj, m.K, res.lat);
  assert(res.nMismatch==0, ['T6-EXACT FALLITO: il VHDL del Tier NON riproduce il blocco (%d/%d) -> non bit-exact RTL.'], ...
         res.nMismatch, res.n);
  fprintf('=== T6-EXACT PASSATO [%s]: Donatello_Tier RTL == blocco su %d/%d ===\n', mode, res.n, res.n);
end

function idx = subset_diverse(mroot)
% 1 traj per combinazione scenario x profilo + estremi di dv + >=1 con cut-in
  ds = load(fullfile(mroot,'test_dataset.mat')); tr = ds.trajectories; N = numel(tr);
  key = strings(N,1); ci = false(N,1); dvmax = zeros(N,1);
  for t=1:N
    key(t) = string(tr{t}.scenario)+"|"+string(tr{t}.profile);
    c = tr{t}.cut_in; ci(t) = any(double(c(:))~=0);
    v = double(tr{t}.val); dvmax(t) = max(abs(v(3,:)));
  end
  [~,ia] = unique(key,'stable'); idx = ia(:).';
  [~,mx] = max(dvmax); [~,mn] = min(dvmax); idx = unique([idx mx mn]);
  if ~any(ci(idx)), idx = unique([idx find(ci,1)]); end
  fprintf('subset diversificato: %d traiettorie -> %s\n', numel(idx), mat2str(idx));
end
```

- [ ] **Step 2: T6-EXACT sul subset (deve PASSARE)**

Run: `matlab -sd "<Harness_SNN>" -batch "run_rtl_validate_tier('reduced')"`
Expected: **PASS** — `T6-EXACT [reduced]: nMismatch = 0 / <n>` poi `=== T6-EXACT PASSATO … ===`.

- [ ] **Step 3: PROVA DI SENSIBILITÀ**

In coda a `run_rtl_validate_tier.m`:
```matlab
function sensitivity_t6()
%SENSITIVITY_T6  1 LSB corrotto sul golden -> T6-EXACT DEVE vedere nMismatch>=1.
  here = fileparts(mfilename('fullpath')); mroot = fullfile(here,'..','..','matlab');
  addpath(mroot); addpath(fullfile(here,'..','common'));
  ROOT='D:/zbd_tier'; hdlsrc=fullfile(mroot,'hdlsrc_donatello_tier');
  tb=fullfile(here,'tb_tier_stream.v'); runner=fullfile(here,'..','common','rtl_run_xsim.sh');
  if ~exist(fullfile(hdlsrc,'Donatello_Tier.vhd'),'file')
    rtl_gen_dut('Donatello_Tier', hdlsrc, 'VHDL', {'TIER','BALANCED','NFRAC','13'});
  end
  tier_export_vectors(1, 'sens', ROOT);
  gf = fullfile(ROOT,'gold_sens.mem'); L = strsplit(strtrim(fileread(gf)), newline);
  v = hex2dec(L{1}); L{1} = sprintf('%06X', bitxor(uint32(v), uint32(1)));
  f = fopen(gf,'w'); fprintf(f,'%s\n', L{:}); fclose(f);
  m = load(fullfile(ROOT,'meta_sens.mat'));
  cmd = sprintf('bash "%s" "%s" "%s" "%s" %d 500 stim_sens.mem gold_sens.mem', runner, ROOT, hdlsrc, tb, m.K);
  [~,out] = system(cmd);
  nm = str2double(regexp(out,'RTLRES nMismatch=(\d+)','tokens','once'));
  assert(nm>=1, 'SENSIBILITA FALLITA: 1 LSB corrotto ma T6-EXACT vede 0 -> cancello CIECO');
  fprintf('=== SENSIBILITA T6-EXACT OK: 1 LSB corrotto -> nMismatch=%d ===\n', nm);
end
```
Run: `matlab -sd "<Harness_SNN>" -batch "run_rtl_validate_tier('reduced'); sensitivity_t6()"`
Expected: T6-EXACT verde, poi `=== SENSIBILITA T6-EXACT OK: 1 LSB corrotto -> nMismatch=1 …`.

- [ ] **Step 4: commit**
```bash
git add FaseB2.0/Harness_SNN/run_rtl_validate_tier.m
git commit -m "feat(b2.0-t6): cancello T6-EXACT (RTL Tier==blocco, subset) + prova di sensibilita (1 LSB)"
```

---

## Task 7: Cancelli **LAT** e **PORT-TYPE**

**Files:** Modify `FaseB2.0/Harness_SNN/run_rtl_validate_tier.m`

- [ ] **Step 1: assert LAT** — in `run_rtl_validate_tier`, dopo `res.lat`:
```matlab
  assert(res.lat > 0 && res.lat < HOLD, ['LAT FALLITO: latenza RTL=%d, HOLD=%d (campiono PRIMA che l''uscita sia pronta).'], res.lat, HOLD);
  fprintf('LAT: latenza RTL misurata = %d clock (< HOLD=%d) OK\n', res.lat, HOLD);
```

- [ ] **Step 2: PORT-TYPE — coperto da T6-EXACT**

`tier_block_params` legge le uscite come `double`, l'exporter le riquantizza a `Q7.13`. Un tipo di uscita diverso
da `Q7.13` darebbe **mismatch sistematico** su ogni control-step → il cancello è **coperto da T6-EXACT** (se
`nMismatch==0`, il tipo è coerente). Nessun codice aggiuntivo; documentato nel README (Task 10).

- [ ] **Step 3: rieseguire (T6-EXACT + LAT)**

Run: `matlab -sd "<Harness_SNN>" -batch "r=run_rtl_validate_tier('reduced'); fprintf('lat=%d\n', r.lat)"`
Expected: `LAT: latenza RTL misurata = <~364> clock (< HOLD=500) OK` + T6-EXACT verde.

- [ ] **Step 4: commit**
```bash
git add FaseB2.0/Harness_SNN/run_rtl_validate_tier.m
git commit -m "feat(b2.0-t6): cancello LAT (latenza RTL misurata < HOLD); PORT-TYPE coperto da T6-EXACT"
```

---

## Task 8: Full-60 — probe di costo del golden, poi background o fallback

**Files:** Create `FaseB2.0/Harness_SNN/probe_golden_cost.m`

- [ ] **Step 1: `probe_golden_cost.m`**

```matlab
function probe_golden_cost()
%PROBE_GOLDEN_COST  Misura il tempo del golden-da-blocco su 1 traiettoria (1000 step) -> stima i 60.
  addpath(fullfile(fileparts(mfilename('fullpath')),'..','..','matlab'));
  t0 = tic; tier_block_params(1, 500); dt = toc(t0);
  fprintf('golden 1 traj (1000 control-step): %.1f s -> stima 60 traj = %.1f min\n', dt, dt*60/60);
  fprintf('DECISIONE: stima <= ~30 min -> full-60 background (Step 2a); altrimenti estrazione-MEX (Step 2b).\n');
end
```
Run: `matlab -sd "<Harness_SNN>" -batch "probe_golden_cost"`

- [ ] **Step 2a: se fattibile — full-60 in background** (redirige su log in `results/`)
```bash
mkdir -p "FaseB2.0/Harness_SNN/results"
"C:/Program Files/MATLAB/R2026a/bin/matlab.exe" -sd "<Harness_SNN>" -batch "r=run_rtl_validate_tier('full'); fprintf('FULL60 nMismatch=%d/%d\n', r.nMismatch, r.n)" > "FaseB2.0/Harness_SNN/results/full60.log" 2>&1
```
Expected (a fine): `T6-EXACT [full]: nMismatch = 0 / 300000` + `FULL60 nMismatch=0/300000`.

- [ ] **Step 2b: se troppo lento — estrazione-MEX** (fallback M1, task a sé)

Se lo Step 1 stima ore: estrai l'algoritmo del Tier@BAL (pattern `extract_champion_algo.m`), codegen MEX,
**cross-check `dmax=0` vs `tier_block_params` sul subset**, poi MEX per il full-60. **Fermati qui**, riporta la
stima e apri il follow-up T6a-bis — non improvvisare l'estrazione senza il cross-check.

- [ ] **Step 3: commit**
```bash
git add FaseB2.0/Harness_SNN/probe_golden_cost.m
git commit -m "feat(b2.0-t6): probe costo golden-da-blocco (decide full-60: background vs estrazione-MEX)"
```

---

## Task 9: Metriche di stima (blocco vs gt_params, con identificabilità)

**Files:** Create `FaseB2.0/Harness_SNN/tier_rtl_metrics.m`

- [ ] **Step 1: `tier_rtl_metrics.m`**

```matlab
function M = tier_rtl_metrics(trajList)
%TIER_RTL_METRICS  Accuratezza stima 5 param (blocco Tier@BAL vs gt_params). Poiche' T6-EXACT prova RTL==blocco,
%  sono le metriche della versione FPGA. max/p99 (coda). NOTA: v0 = identificabilita (flusso libero), non RTL.
  if nargin<1||isempty(trajList), trajList = 1:60; end
  here = fileparts(mfilename('fullpath')); mroot = fullfile(here,'..','..','matlab'); addpath(mroot);
  ds = load(fullfile(mroot,'test_dataset.mat')); tr = ds.trajectories;
  P = tier_block_params(trajList, 500);
  names = {'v0','T','s0','a','b'}; err = [];
  for i = 1:numel(trajList)
    gt = double(tr{trajList(i)}.gt_params(:)).';
    err = [err; abs(P{i} - gt)]; %#ok<AGROW>
  end
  M = struct();
  fprintf('Accuratezza stima param (versione FPGA == blocco, %d control-step):\n', size(err,1));
  for i=1:5
    M.(names{i}) = struct('max',max(err(:,i)),'p99',prctile(err(:,i),99));
    fprintf('  %-3s  max=%.4g  p99=%.4g\n', names{i}, M.(names{i}).max, M.(names{i}).p99);
  end
  fprintf('NOTA: v0 alto = identificabilita (v0 osservabile a flusso libero), non difetto RTL.\n');
end
```
Run: `matlab -sd "<Harness_SNN>" -batch "tier_rtl_metrics(1:60)"`
Expected: max/p99 per v0,T,s0,a,b (finiti; `v0` più alto = identificabilità).

- [ ] **Step 2: commit**
```bash
git add FaseB2.0/Harness_SNN/tier_rtl_metrics.m
git commit -m "feat(b2.0-t6): tier_rtl_metrics - accuratezza stima param (con nota identificabilita)"
```

---

## Task 10: README dell'harness + doc di stato + chiusura T6a

**Files:** Create `FaseB2.0/Harness_SNN/README.md` · Modify `document/HDL_PHASE.md`, `document/SESSION_RESUME.md`, `FaseB2.0/README.md`

- [ ] **Step 1: `Harness_SNN/README.md`**
```markdown
# Harness_SNN (T6a) — validazione RTL di Donatello_Tier@BALANCED
DUT: `Donatello_Tier` @ TIER=BALANCED, NFRAC=13. Golden = il blocco stesso (`matlab/tier_block_params.m`).
Work-dir xsim: `D:/zbd_tier` (corta, senza spazi). Utilità condivise: `../common/{rtl_write_vectors.m,rtl_run_xsim.sh}`.

## Comandi (da questa cartella, `-sd`)
- Gen VHDL: `rtl_gen_dut('Donatello_Tier',[],'VHDL',{'TIER','BALANCED','NFRAC','13'})` (in `matlab/`)
- T6-EXACT + sensibilità: `run_rtl_validate_tier('reduced'); sensitivity_t6()`
- Full-60 (dopo probe): `probe_golden_cost` → `run_rtl_validate_tier('full')`
- Metriche: `tier_rtl_metrics(1:60)`

## Cancelli
- **T6-EXACT** (RTL 5 param == blocco, `nMismatch==0`; copre **PORT-TYPE**) · **LAT** (~364 < HOLD=500) · sensibilità 1 LSB.

## Prodotti
`D:/zbd_tier/` (stim/gold `.mem`, xsim — fuori dal repo); `results/full60.log` (tenuto).
```

- [ ] **Step 2: doc di processo** — in `document/HDL_PHASE.md` §6:
```
- ✅ **[FASE B2.0 T6a] Harness_SNN (Donatello_Tier@BALANCED) validato a livello RTL**: il VHDL generato riproduce
  bit-exact il BLOCCO (golden-da-blocco tier_block_params) — T6-EXACT nMismatch 0/<n> su subset (8 combinazioni
  scenario×profilo + estremi dv + cut-in) + full-60 <esito>; sensibile a 1 LSB; latenza ~364 < HOLD. Struttura:
  golden in matlab/, utilita in FaseB2.0/common/, banco in FaseB2.0/Harness_SNN/, xsim in D:/zbd_tier. HW = T6b.
```
In `document/SESSION_RESUME.md` (blocco FASE B2.0) e `FaseB2.0/README.md`: spunta **T6a**, prossimo **T6b** (HW).

- [ ] **Step 3: commit + verifica**
```bash
git add FaseB2.0/Harness_SNN/README.md document/HDL_PHASE.md document/SESSION_RESUME.md FaseB2.0/README.md
git commit -m "docs(b2.0-t6): Harness_SNN validato a livello RTL (T6a chiuso) + stato aggiornato"
git status --short
```
Expected: pulito salvo `clockInfo.txt` e nessun `hdlsrc_donatello_tier/` (gitignorato); `D:/zbd_tier` è fuori dal repo.

---

## Self-Review

**Spec coverage (§ spec → task):** golden blocco-oracolo→Task 2 · gen DUT+variante BAL→Task 1 · utilità condivise→Task 3 ·
exporter→Task 4 · TB→Task 5 · T6-EXACT+sensibilità→Task 6 · LAT+PORT-TYPE→Task 7 · full-60→Task 8 · metriche→Task 9 ·
doc→Task 10. **HW (6a/6b/6c) = piano T6b** (dichiarato).

**Struttura (vincolo B2.0):** golden `tier_block_params`→`matlab/` ✓ · utilità generiche→`common/` ✓ · banco (tb/run/exporter/metriche/probe/README)→`Harness_SNN/` ✓ · xsim in `D:/zbd_tier` (no spazi) ✓.

**Placeholder scan:** nessun TBD; nomi porta = passo concreto (Task 1); full-60 = ramo esplicito (Task 8).

**Type consistency:** `numerictype(1,21,13)`, `HOLD=500`, `fixdt(1,32,20)`, mask 21b/32b, `{'TIER','BALANCED','NFRAC','13'}`,
`ROOT='D:/zbd_tier'`, campionamento `idx=hold*(0:K-1)+lat+1` coerenti oracolo↔TB↔orchestratore. `rtl_write_vectors(outdir,tag,stim,32,gold,21)` firma stabile.

---

## Execution Handoff

T6a pronto (10 task). Esecuzione **inline** (`executing-plans`), a checkpoint. Job MATLAB/xsim lunghi gestiti come background-job con lettura dei file di output.
