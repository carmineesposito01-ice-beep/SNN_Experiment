# Fase B2.0-2d — Ottimizzazione timing SNN→decode (design)

**Data:** 2026-07-18
**Branch/worktree:** `Simulink_Importer`
**Stato:** design approvato in brainstorming — da tradurre in piano (`writing-plans`)
**Contesto:** dopo 2b Esp.A (A1 tanh-LUT integrata: il `tanh` non è più il collo, controllore a **10,58 MHz**), il
collo è ora il path **SNN→decode** (`pR_idx → pv_3`, **172 livelli**). Questo studio lo attacca per spingere
l'Fmax del controllore **il più in alto possibile, bit-exact** — la SNN è ora aperta all'ottimizzazione (congelata
resta solo la **funzione**, `dmax=0`, come per A1).

---

## 1. Obiettivo

Massimizzare l'`Fmax` OOC del controllore `Donatello_ACC_IIDM_M` **bit-exact** (`dmax=0`), ottimizzando il path
SNN→decode. **Non c'è target fisso**: si scopre il **tetto HW reale** della SNN. Sfrutta il fatto che la SNN è
**time-multiplexata** (1 neurone/clock, ~320 clock/inferenza): pipelinare il compute per-neurone **aggiunge
latenza ma non costa throughput** (un control-step dura 800.000 clock) — esattamente come il `tanh` (pipeline
gratis in latenza → 136 MHz). Il "limite 11,65" era il tetto *non-pipelinato-per-neurone*, non un limite hardware.

## 2. Il path (172 liv) e un finding già emerso

Il critpath `pR_idx → pv_3` attraversa **due metà**:
- **Stadio C** (`snn_b2_fsm`, compute per-neurone): ROM indicizzate da `pR_idx` + **~41 MAC** (il `reci` = somma di
  16 prodotti `W.U·t_lr` è il pezzo più profondo) + leaky + soglia + accumulo readout → `V_LI` → `raw`.
- **Decode** (`snn_decode_lut`): per-parametro (5, srotolati), ~4 moltiplicazioni in serie + LUT sigmoide + interp.

**🔎 Finding (probabile round 1, "frutto basso").** Nel chart del blocco M la sequenza è
`if valid { rawl=raw; phase=1 }` seguito da `if phase==1 { pv=decode(rawl) }`, ma i due `if` sono **separati**: nel
ciclo in cui `valid` diventa vero, `phase` è messo a 1 **e** il secondo `if` lo rilegge subito → **il decode gira
nello STESSO ciclo del readout**. Ecco perché il critpath è `pR_idx→pv` **fuso** (172 liv = readout SNN **+** decode
in un unico ciclo combinatorio). Rendendo i due **stati mutuamente esclusivi** (integrare `valid` nella catena
`elseif` degli stati) il decode passa al ciclo successivo → `rawl` diventa un **vero registro** → il path si spezza
in `max(readout, decode)` invece della somma. **+1 ciclo di latenza, bit-exact.** È il candidato al primo salto.

## 3. Approccio: probe-first iterativo (strada A), con B/C come fallback

Deciso dall'utente. Ciclo, ripetuto finché converge:

1. **Sonda** il collo corrente (§4): quanto è stadio-C vs decode, e *dove* dentro.
2. **Applica la leva che calza** (§5) al collo, **bit-exact**.
3. **Ri-sintetizza** OOC → nuovo `Fmax` + nuovo critpath + Δrisorse.
4. **Verifica `dmax=0`** (§7).
5. **Ripeti** — il collo si sposta; ogni round lo re-identifica sui dati (non a indovinare).

> Perché iterativo (a differenza del `tanh`, un muro singolo): qui i colli sono **multipli** (stadio-C e decode, e
> più contributi dentro ciascuno). Pipelinarne uno sposta il collo al successivo. Si converge scoprendo il collo a
> ogni giro. Le strade **B** (DSP-centrica) e **C** (decode-LUT) sono le leve di **escalation** quando l'FSM-staging
> non basta per un dato collo (§5).

## 4. Sonda di decomposizione

Per sapere *dove* sono i livelli prima di scegliere la leva (fondamento di ogni round):
- **DUT standalone** (come L1 del `tanh`, `build_tanhdut`-style): **SNN-sola** (`snn_b2_fsm` → `raw`, I/O registrato)
  e **decode-solo** (`snn_decode_lut` → `pv`, I/O registrato) → `Fmax` intrinseco + critpath + livelli di ciascuno.
- **`critpath.rpt`** della sintesi del controllore intero (già prodotto dal flusso OOC): elenca le **celle** del
  path critico → conteggio stadio-C vs decode, e il punto esatto.
- Riuso il flusso OOC space-free `zbd_pipe` + `synth_acc_iidm.tcl` (top `DUT`).

## 5. Le leve, per collo atteso

| collo | leva primaria (A) | escalation |
|---|---|---|
| readout+decode fusi | **stati mutuamente esclusivi** (§2): +1 registro fra readout e decode | — |
| Stadio C (ROM + ~41 MAC, `reci` 16-termini) | **sotto-staging FSM**: spezzo il compute del neurone su più cicli (serializzo il `reci` e/o gli accumuli) | **B**: MAC nei registri interni **DSP48** (cascata P/PCIN), non nel fabric |
| Decode (moltiplicazioni in serie + sigmoide) | **registri fra le moltiplicazioni** in serie | **C**: **decode-LUT memoizzata** (come il `tanh`: bit-exact per costruzione) |

Tutte le leve preservano la **funzione** (`dmax=0`): FSM-staging e DSP-pipeline aggiungono solo latenza; una
decode-LUT deve **memoizzare** i valori esatti (come A1), non approssimare.

## 6. Criterio di convergenza

Stop quando **una** delle seguenti:
- il collo è un'**operazione singola non spezzabile** bit-exact (es. un moltiplicatore DSP già pipelinato al massimo);
- un round rende **< ~5%** di `Fmax`;
- si sfora un **budget di risorse** dichiarato (la pipeline costa registri/area — da fissare nel piano).

Ogni round logga `Fmax` + critpath + Δ{LUT,FF,DSP,BRAM} + latenza. Il verdetto finale: la curva `Fmax` per round,
il tetto raggiunto, il costo risorse, il collo residuo (perché non oltre).

## 7. Non-regressione bit-exact (ad ogni round)

- **`run_b2_parity_dataset` = 0/240000** — il gate **duro** del core SNN, che qui **stiamo davvero ristrutturando**
  (a differenza di 2b, dove la SNN non si toccava). È il guardiano n.1.
- **B-1** (controllore RTL) + **A-1** (SNN sola) + **PLANT-PAR** + **B-LOOP** (car-following) — `dmax=0`.
- **Latenza:** ogni round aggiunge qualche ciclo; cumulativa resta ≪ inferenza (~320 clock) e va **misurata e
  propagata** (bump di `HOLD` nell'harness se supera 500). Il TB campiona a fine finestra.
- **Ambiente:** harness xsim con **Git Bash in testa al PATH** (`bash`→WSL è rotto — vedi `SP4 §Studio 2b`).

## 8. Rischi

1. **Ristrutturazione del core SNN.** Bit-exact ma delicato: il `reci`/`Ii`/soglia/spike sono l'aritmetica di
   `snn_core` (mirror bit-exact). Il parity **0/240000** è il guardiano duro; ricordare il bug **§2.1** (un cast
   prematuro sul potenziale costò l'82,4% dei control-step). Ogni round va provato sul dataset, mai su caso singolo.
2. **Latenza cumulativa** → `HOLD`: misurare, non assumere.
3. **Mapping DSP** (fallback B) incerto: lo misura la sonda; HDL Coder potrebbe non pipelinare i MAC come sperato
   (come per il `tanh` automatico in F1) → si valuta round per round.
4. Ogni round è **indipendente** e lascia il core valido + bit-exact + sintetizzabile.

## 9. Criteri di successo

- Sonda di decomposizione eseguita: sappiamo dove sono i 172 livelli.
- ≥1 round di pipelining bit-exact con **`Fmax` misurato OOC in salita** e **parity 0/240000** + gate verdi.
- Curva `Fmax`(round) + verdetto: tetto raggiunto, costo risorse, collo residuo.
- `Donatello_ACC_IIDM_M` aggiornato (bit-exact), doc allineati (`SP4 §Studio 2d`, `SESSION_RESUME`, memoria).

### Fuori scope

- Modifiche che **approssimano** la funzione della rete (viola `dmax=0`). — Il fronte `tanh` (chiuso in 2b). —
  Il gate esaustivo di 2b rinviato (resta da fare, indipendente). — La validazione full-dataset 60k (2c),
  a valle. — Studi di quantizzazione (meno bit) — sono un fronte a sé, non questo.
